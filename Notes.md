# Lumen Tools 修复与优化记录

日期：2026-08-05

依据：`docs/tool-improvement-plan.md`

## 本轮结果

本轮以改进清单中的共用 P1 问题、格式转换正确性、安全边界和代码可读性为主。原文中尚未实施的长期 P2 数据扩充、跨工具联动和历史记录能力仍保留为后续清单，不在这里标记为已完成。

### 共用编辑器与代码高亮

- `EditorPanel` 接入 Prism 语法高亮，自动识别 JSON、YAML、HTML/XML/SVG、CSS、JavaScript、TypeScript、SQL、Shell、Markdown 和 TOML。
- 深色和浅色主题分别配置语义色：注释、属性、数字、字符串、操作符、关键字、函数和变量保持可区分的对比度。
- 编辑层与高亮层共享字体、行高、内边距和滚动位置；编辑器仍可垂直拖动高度，长行在编辑器内部横向滚动，不撑开页面。
- 代码输入支持 Tab 插入两个空格；语言标签显示在面板标题栏，便于确认识别结果。
- 只读输出增加统一空态；错误不再写入可复制输出，复制按钮在无内容时禁用，并显示成功或失败反馈。
- 剪贴板 API 不可用时增加兼容回退，并对浏览器拒绝剪贴板权限给出失败状态。

### 格式、编码与转换工具

- JSON 工作台：输入变化后清除旧结果；错误与输出分离；尽可能补充错误行列；格式化结果强制按 JSON 高亮。
- YAML 转 JSON / YAML 查看器：由平面键值解析升级为完整 YAML 解析，支持数组、嵌套对象、布尔值、数字、空值和行列错误提示。
- JSON 转 YAML：使用完整序列化器，支持嵌套、数组、多行内容和类型保留；修改输入后清除旧结果。
- Base64：移除过时的 `escape/unescape` Unicode 处理，改用 `TextEncoder/TextDecoder`；支持 URL-safe Base64、空白清理、非法字符/长度和非法 UTF-8 提示。
- URL 编解码：增加“参数/组件”和“完整 URL”两种处理范围；输入或模式变化时清除旧结果。
- HTML/XML 格式化：XML 在格式化前使用 `DOMParser` 校验；错误进入状态区，空输出不可复制。
- SQL 格式化结果显式启用 SQL 高亮。
- Markdown 转 HTML：原始 HTML 默认转义，脚本和事件属性不会在预览区执行。

### 差异、文本与参考工具

- JSON Diff：相同 JSON 显示明确空态；支持 3 行或全部上下文；增加大输入复杂度保护，避免 LCS 表占用过量内存；保留新增/删除统计和复制结果。
- 正则测试器：增加全局、忽略大小写、多行开关，并展示捕获组内容；保留高级 flags 输入和表达式错误态。
- 文本统计：按 Unicode grapheme 统计 Emoji/组合字符，使用中英文分词规则，并增加 UTF-8 字节数。
- Git 命令备忘：改为自动保存到本机 `localStorage`，增加复制全部。

### 安全与资源生命周期

- Basic Auth：使用 UTF-8 字节生成 Base64，只输出可直接复制的完整 `Authorization` Header，并提示 Base64 不等于加密。
- 摄像头录制：增加权限拒绝和不支持状态；显示录制时长；停止、离页或重新生成结果时释放媒体轨道与 Object URL。
- 数学表达式：补充除零、取模零、剩余非法 token 和非有限结果校验。
- ETA：校验总量、完成量和耗时边界，避免负进度、超过 100% 或无效估算。

## 第二轮继续处理

### 规范解析器与构建体积

- TOML：接入 `smol-toml`，按 TOML 1.1 解析和序列化；支持嵌套表、数组表、日期、转义和类型保留，错误与结果分离。
- HTML：接入 Prettier HTML 插件替代手写缩进；格式化器按需加载，避免进入其他工具时承担整包成本。XML 继续在格式化前执行结构校验。
- SQL：接入 `sql-formatter`，支持标准 SQL、MySQL、PostgreSQL、SQLite、BigQuery 和 SQL Server 方言，以及关键字大写、小写和保持原样。
- 构建：对 React、格式解析器、Prettier 和 SQL 格式化器进行分包；Prettier/SQL 仅在使用对应工具时加载，生产构建不再产生超大 chunk 警告。

### 图片与文件工具

- 增加共用拖拽/选择文件组件：支持重复选择同名文件、类型检查、文件大小限制和明确错误状态。
- 图片转 Base64：支持 Data URI 与纯 Base64 切换，显示文件名和字节大小，15 MB 上限。
- 图片格式转换：使用浏览器本地解码和 Blob 导出，支持 PNG/JPEG/WebP、质量调节、原图与结果大小、JPEG 透明区域背景色；重复生成和离页时释放 Object URL。
- 图片转换在设置改变但尚未重新生成时显示提示，下载文件名始终以实际生成格式为准，避免扩展名与文件内容不一致。
- Favicon：支持 16/32/64/128/256 多尺寸 PNG，保留透明背景，并生成与下载文件一致的 `link` 标签。
- 文件转 Base64：支持拖拽、Data URI/纯 Base64、20 MB 上限和文件信息；哈希工具支持 50 MB 以内本地文件、Hex/Base64 输出。

### 编码、安全与边界输入

- AES：密码经 PBKDF2-SHA256（210,000 次、随机 16 字节 Salt）派生 AES-256-GCM 密钥；每次使用随机 12 字节 IV，并将版本、Salt、IV 和密文封装为 Base64。补充密文格式校验和统一解密失败提示。
- JWT：严格检查三段结构、Base64URL 字符和 UTF-8/JSON；Header、Payload 和签名单独展示与复制，并明确提示“只解码、不验签”。
- 密码生成器：使用拒绝采样消除随机字符取模偏差，补充熵值估算；随机端口改用 `crypto.getRandomValues`。
- 进制转换：以 `BigInt` 支持任意精度整数、负数及严格数字校验；罗马数字增加规范顺序和减法规则校验。
- 文本与二进制：严格要求 8 位字节并使用 fatal UTF-8 解码，非法字节不再静默替换。
- JSON/CSV/XML：补充 CSV BOM 下载、XML 根节点与数组映射、属性/混合文本保留、标签名与文本转义以及错误态清理。
- docker run 转 Compose：使用 shell tokenizer，覆盖多端口、环境变量、卷、网络、重启策略和命令参数。
- 时间与数值：时间戳支持秒/毫秒、本地/UTC、负时间戳和日期反向转换；温度增加绝对零度与精度设置。

## JSON 工作台专项优化（2026-08-11）

- 缩进支持 2 空格、4 空格和 Tab；格式化结果及状态栏同步显示当前策略。
- 增加递归属性排序：对象的每一层按键名排序，数组顺序保持不变；格式化与压缩均可使用。
- 共用 `EditorPanel` 增加可选行号，输入和结果滚动时与高亮层同步，JSON 工作台可随时开关。
- 增加自动格式化开关：输入停止 320ms 后解析有效 JSON 并刷新结果，避免逐键处理和输入光标跳动；无效 JSON 继续显示行列错误。
- 工具栏保持紧凑布局，窄屏允许自然换行，不增加独立设置面板。

## 结果视图与全网页模式（2026-08-11）

- `EditorPanel` 的只读结果不再渲染 `textarea`，统一改为带可访问名称的 `div` 结果区域；输入区继续保留可编辑文本域。
- 结果区域保留 Prism 高亮、复制、空态与可选行号；多行内容按实际高度完整展开，超长单行只在结果内部横向滚动，不撑宽页面。
- 工具页壳层自动识别 `.dual-editor` 和 `.json-diff-inputs`：左输入/右结果、左右双输入后接结果以及 JSON Diff 等布局统一获得“全网页”入口。
- 通用全网页模式支持按钮与 Esc 退出、切换工具自动复位、工作区独立滚动；图片工作室继续沿用专用全屏布局。
- 全网页入口放在克制的工作区视图栏中，避免覆盖工具按钮、复制操作或编辑内容。

## 图片转换与图片 Base64（2026-08-11）

- 图片转 Base64 的结果区启用强制换行，Data URI 和纯 Base64 长字符串不会产生横向页面滚动。
- 共用文件导入组件增加可选剪贴板图片监听；图片格式转换、图片转 Base64 均支持 Ctrl/⌘V，复用拖拽导入的格式与 15 MB 大小校验。
- 图片格式转换设置栏移动至导入和预览区域上方；移除“应用设置”，输出格式、质量和 JPEG 背景变化后以 120ms 防抖自动更新预览。
- 自动转换使用请求序号隔离并发结果，快速调整时旧转换不会覆盖新预览；失败、重新导入或离页时释放 Blob URL。
- 下载入口移动至原“应用设置”位置；仅在当前设置的预览生成完成后可用，文件扩展名与实际输出格式保持一致。

## Cron 表达式专项优化（2026-08-11）

- Cron 工具拆分为独立模块，默认载入参考表达式 `23 0-20/2 * * *`；保留常用预设，并增加本地时区与 UTC 切换。
- 建立严格的五字段校验：覆盖分钟、小时、日期、月份、星期的取值范围，以及通配符、列表、范围和正整数步长；明确拒绝 Quartz 的 `?`、`L`、`W`、`#` 和多余秒字段。
- 月份和星期支持大小写不敏感的三字母英文缩写，例如 `JAN-MAR`、`MON-FRI`，解析结果统一显示为中文。
- 输入下方增加字段令牌与五段拆解区；越界、零步长、格式错误、缺失和多余字段会精确标红，并提供中文错误原因。日期与月份组合不存在时会同时高亮两个关联字段。
- 增加中文自然语言解析；日期与星期同时受限时明确提示“任一条件匹配”，避免误解标准 Cron 的 OR 语义。
- 使用当前时区计算未来 5 次执行时间；输入、预设或时区变化后实时刷新，无法产生有效日期的表达式不会显示误导性结果。
- 界面重构为单一主输入、五字段校验带、中文解析和后续执行时间四个层级；桌面端保持横向快速扫描，窄屏自动折叠字段与结果区域。

## 多工具工作台扩展（2026-08-11）

- URL 解析器：支持省略协议的域名输入，拆解协议、认证、主机、端口和中文路径；查询参数与 Hash 参数逐项解析，保留重复参数并自动显示百分号编码对应的汉字。
- URL 编解码：在单个参数和完整 URL 之外，增加“URL 中的参数值”模式，只转换查询与 Hash 参数值，不破坏协议、路径、键名和分隔符。
- SVG 占位图：增加宽度、高度、字号、文本、背景色、文字色和字体设置；实时预览并支持复制 SVG、下载 SVG 和下载 PNG。
- Favicon：尺寸按钮文本改为水平垂直居中；下载按钮移动到尺寸按钮同一行，并随当前输出尺寸更新文件名。
- YAML 查看器升级为 YAML 工作台：支持 2/4 空格缩进、递归属性排序、自动格式化，以及在美化 YAML 和 JSON 视图之间切换。
- JSON Diff 与文本 Diff：均增加隐藏相同内容，并保留完整差异文本复制能力；JSON Diff 的上下文选择在隐藏相同内容时自动禁用。
- 正则备忘录扩展至 200 条，按日期时间、网络地址、联系方式、姓名账户、数字金额、中文文本、编程格式、文件路径、表单业务、安全校验及长度规则等分类检索。
- 正则测试器参考 regex-vis 的可视化编辑思路，增加语法轨道，拆解分组、字符类、量词、转义、锚点和分支；补齐 `s`、`u`、`y` 等常用标志。
- Git 命令备忘扩充为 72 条分类命令，覆盖仓库配置、暂存、提交、分支、远程协作、变基、储藏、标签和排查维护，并为每条命令增加中文说明。
- Lorem Ipsum：支持按段落、句子或单词生成，并可选择是否从经典 `Lorem ipsum` 开头。
- 新增二维码创建：支持内容、尺寸、留白、背景色、色块颜色、默认/上传中心图片、图片色块遮罩和边框，使用高容错级别实时生成，可下载或复制 PNG。
- 新增 Mermaid 图表编辑器：停止输入 280ms 后自动渲染，支持常见 Mermaid 图表语法，并可复制/下载 SVG 或下载 PNG；依赖按需加载，不阻塞其他工具首屏。
- 新增工具继续遵循单一主工作区和紧凑设置原则，桌面端以输入/预览并列为主，窄屏自动折叠为单列。

## 依赖变化

- 新增 `prismjs` 与 `@types/prismjs`：共用编辑器语法高亮。
- 新增 `yaml`：完整 YAML 解析与序列化。
- 新增 `smol-toml`：TOML 1.1 解析与序列化。
- 新增 `prettier`：HTML 规范格式化，运行时按需加载。
- 新增 `sql-formatter`：多方言 SQL 格式化，运行时按需加载。
- 新增 `cron-parser`：校验跨字段日期组合，并计算带时区和夏令时语义的后续执行时间。
- 新增 `qrcode` 与 `@types/qrcode`：在浏览器画布生成可配置二维码。
- 新增 `mermaid`：解析并渲染 Mermaid 图表，运行时按需加载。

## 验证结果

- `npm run build`：通过。
- `npm run lint`：通过。
- `git diff --check`：通过。
- Chromium 桌面视口：JSON 输入/输出高亮、深浅主题、空态和错误态通过；控制台无错误；页面无横向溢出。
- 390×844 移动视口：双栏正确折叠为单栏，编辑器宽度受容器约束，页面无横向溢出。
- JSON 工作台：有效/无效 JSON、旧结果清理、可复制输出、2/4 空格与 Tab、递归属性排序、自动格式化开关、手动压缩、行号开关及多行数量一致性通过。
- YAML：嵌套对象、对象数组、行内数组、布尔值以及无效 YAML 错误态通过。
- Base64：`你好👋` 的标准/URL-safe 编码与解码往返通过。
- JSON Diff：相同内容空态、键值修改、上下文行和统计通过。
- TOML：嵌套表、数组表、日期、转义和无效输入状态通过。
- HTML/SQL：按需加载的格式化流程、多方言/大小写选项和错误态通过。
- 图片：文件类型/大小、重复选择重置、格式状态与 Object URL 释放路径通过代码检查；390×844 下转换控件无横向溢出。受本地浏览器测试驱动的文件选择器限制，本轮未把真实文件导入记作自动化交互通过。
- AES：加密/解密往返、错误密码和非法密文状态通过。
- JWT、BigInt 进制、严格二进制、Docker Compose 与时间戳边界输入通过。
- Prism：SQL 输入与输出均生成语义 token；移动端 SQL、时间戳和图片转换页面无横向溢出；浏览器控制台无警告或错误。
- JSON 工作台 390×844 移动视口：设置工具栏自然折行，页面与工具栏均无横向溢出；浏览器控制台无警告或错误。
- 结果/全网页：JSON 高亮结果和 Base64 普通文本结果均无只读 `textarea`；45 行以上结果完整展开。JSON 工作台、文本 Diff、JSON Diff 的全网页入口、Esc/按钮退出和路由复位通过。
- 390×844 移动视口：超长结果保持在结果区内横向滚动，全网页工作区高度等于视口，页面无横向溢出；浏览器控制台无警告或错误。
- 图片剪贴板/转换：PNG 剪贴板粘贴在两个工具中通过；Base64 结果为 `pre-wrap/anywhere`；WebP → JPEG 设置变化会自动替换预览 URL 和下载格式，设置栏位于导入/预览上方且无“应用设置”按钮。
- Cron：参考表达式、分钟越界、零步长、缺失/多余字段、无效日期组合、`JAN`/`MON-FRI` 缩写、日期/星期 OR 语义、本地/UTC 切换及未来 5 次时间通过；错误令牌与关联字段高亮符合预期。
- URL/YAML/Diff：中文查询与 Hash 参数、重复参数、参数值编码、YAML 递归排序与 JSON 切换、JSON Diff 隐藏相同内容均通过；URL 页面无横向溢出。
- 正则/Git：正则库数量确认为 200 条，“英文名称”搜索与分类筛选通过；正则测试器默认表达式生成 12 个可视化语法节点并保持 2 个匹配；Git 命令分类和复制入口通过结构检查。
- 生成与图表：二维码默认画布为 360×360 且下载状态可用；SVG 占位图 7 项设置、实时预览和 3 个复制/下载入口通过；Mermaid 默认流程图生成 SVG 和 5 个节点；URL 参数值编码输出正确。

## 仍保留的后续工作

- 文档中 P2 参考数据扩充、跨工具联动、历史记录和批量下载能力尚未实施。
- 图片裁剪与拼接仍可增加撤销/重做、裁剪比例、触控缩放；摄像头仍可增加输入设备选择。
- cURL 转 fetch 已覆盖常用 Header、JSON Body 和认证参数，但尚未承诺兼容完整 shell 语法及全部 cURL 选项。
- 个别工具仍可继续增加更深入的专用能力，例如 JSONPath/括号补全、文本词级 Diff、RSA 密钥下载和 OTP URI 解析。
- 当前验证以构建、静态检查和浏览器冒烟为主；若继续扩大功能面，建议为解析器、安全封装和文件导入建立自动化回归用例。
